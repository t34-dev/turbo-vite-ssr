{
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/api/server" }],
  "buildCommand": "cd apps/web && pnpm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install"
}
